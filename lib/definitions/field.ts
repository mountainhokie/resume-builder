export type FieldProps = {
  id: string;
  label: string;
  required: boolean;
  inputType: string;
  placeholder: string;
  icon?: string;
  column?: string; // full, half, third,
  note?: string;
};
